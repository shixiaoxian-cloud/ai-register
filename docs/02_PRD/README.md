# PRD Workspace

本目录用于承接 `ai-pm` 输出的产品需求文档：

- `framework-prd.md`：跨模块、跨版本共享的主 PRD，沉淀背景、价值范围、核心用户旅程、页面树和实体。
- 未来的 feature 级 PRD：按具体功能拆分，承接框架 PRD 的局部细化。

与 `openspec` 的边界：

- `docs/02_PRD/` 负责说明“产品要什么、用户怎么用、为什么这样设计”。
- `openspec/changes/` 负责说明“本次变更怎么落、怎么拆任务、如何实现和归档”。
